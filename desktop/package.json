{
  "name": "@mediaplayer/desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "node scripts/dev.js",
    "build": "vite build && vite build --config vite.web.config.js",
    "start": "npm run build && electron .",
    "build:web": "vite build --config vite.web.config.js"
  },
  "dependencies": {
    "@mediaplayer/server": "*"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "electron": "^33.2.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vite": "^6.0.7"
  }
}
